虛擬碼: 建立開發運維框架新命令骨架

輸入:
  命令名稱: 文字
  專案目錄: 文字 ( 可為空 )

常數:
  框架快取標記相對路徑 = "cache/devops-cli-fwk"
  命令設定根目錄名稱 = "conf/devops"

主流程 建立開發運維框架新命令(命令名稱, 專案目錄)
  驗證命令名稱是否存在(命令名稱)

  目前專案根目錄 ← 取得目前工作目錄()
  專案根目錄 ← 確認專案目錄(專案目錄, 目前專案根目錄)

  確認框架已安裝(專案根目錄)

  建置命令骨架(專案根目錄, 命令名稱)

  顯示訊息("命令骨架已建立: " + 組合路徑(專案根目錄, 命令設定根目錄名稱, 命令名稱))
結束主流程


函式 驗證命令名稱是否存在(命令名稱)
  IF 命令名稱為空 THEN
    顯示錯誤("缺少必要參數: 命令名稱")
    終止流程(失敗)
  END IF
結束函式


函式 確認專案目錄(指定目錄, 目前專案根目錄) 回傳 路徑
  IF 指定目錄為空 THEN
    RETURN 目前專案根目錄
  END IF

  指定目錄絕對路徑 ← 轉換為絕對路徑(指定目錄)
  IF 指定目錄絕對路徑不存在 THEN
    顯示錯誤("指定專案目錄不存在: " + 指定目錄)
    終止流程(失敗)
  END IF

  IF 指定目錄絕對路徑不等於目前專案根目錄 且 指定目錄絕對路徑不屬於目前專案根目錄之子目錄 THEN
    顯示錯誤("指定專案目錄超出允許範圍: " + 指定目錄)
    終止流程(失敗)
  END IF

  RETURN 指定目錄絕對路徑
結束函式


函式 確認框架已安裝(專案根目錄)
  框架標記路徑 ← 組合路徑(專案根目錄, 框架快取標記相對路徑)
  IF 框架標記路徑不存在 或 框架標記路徑非目錄 THEN
    顯示錯誤("開發運維框架尚未安裝")
    終止流程(失敗)
  END IF
結束函式


函式 建置命令骨架(專案根目錄, 命令名稱)
  命令設定根目錄 ← 組合路徑(專案根目錄, 命令設定根目錄名稱)
  若目錄不存在則建立(命令設定根目錄)

  命令目錄 ← 組合路徑(命令設定根目錄, 命令名稱)
  若目錄不存在則建立(命令目錄)

  寫入文字檔(組合路徑(命令目錄, "main.yml"), 主設定檔預設內容, 換行格式為LF)
  寫入文字檔(組合路徑(命令目錄, "postaction.sh"), postaction檔案預設內容, 換行格式為LF)
  寫入文字檔(組合路徑(命令目錄, "action.sh"), 主處理腳本預設內容, 換行格式為LF)
  寫入文字檔(組合路徑(命令目錄, "preaction.sh"), preaction檔案預設內容, 換行格式為LF)
結束函式


預設內容常數:
  主設定檔預設內容 = `desc: "New shell command."`
  postaction檔案預設內容 = `# 後處理腳本，執行前需確認的內容撰寫於此。`
  主處理腳本預設內容 = `# 處理腳本，主要執行的內容撰寫於此。`
  preaction檔案預設內容 = `# 前處理腳本，執行後需確認的內容撰寫於此。`
